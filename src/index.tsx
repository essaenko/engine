import * as React from 'react';
import * as ReactDOM from 'react-dom';
import { App } from 'js/interface/App';

document.addEventListener('DOMContentLoaded', () => {
  ReactDOM.render(<App />, document.getElementById('app'));
});